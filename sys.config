[{kernel,
  [{logger_level, all},
   {logger,
    [{handler, default, logger_std_h,
      #{level => debug,
	config => #{file => "/opt/local/www/logs/webapi.log"},
	formatter => {logger_formatter, #{}}}}]}]},
 {inets,
  [{services,
    [{httpd,
      [{proplist_file,
	"/opt/local/devel/erlang/webapi/httpd.config"}]}]}]},
 {api_app,
  [{routes, "/opt/local/devel/erlang/webapi/routes.config"}]}
].
